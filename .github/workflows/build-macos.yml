name: Build macOS App

on:
  pull_request:
    branches: [ main ]
  push:
    tags: [ 'v*' ]

jobs:
  build:
    runs-on: macos-15
    permissions:
      pull-requests: write
      contents: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Build macOS app
      run: |
        xcodebuild \
          -project daemonic.xcodeproj \
          -scheme Daemonic \
          -configuration Release \
          -derivedDataPath build \
          -sdk macosx \
          ARCHS="arm64 x86_64" \
          ONLY_ACTIVE_ARCH=NO \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGN_STYLE=Manual

    - name: Create app directory
      run: |
        mkdir -p "daemonic_build"
        cp -r build/Build/Products/Release/Daemonic.app daemonic_build/

    - name: Create ZIP archive
      run: |
        cd daemonic_build
        zip -r ../Daemonic.zip Daemonic.app
        cd ..

    - name: Upload build artifact
      uses: actions/upload-artifact@v4
      with:
        name: Daemonic
        path: Daemonic.zip
        retention-days: 30

    - name: Upload to GitHub Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: Daemonic.zip

    - name: Comment on PR with artifact download link
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        script: |
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: `📦 Build artifact available: [Daemonic.zip](https://github.com/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId})`
          });
